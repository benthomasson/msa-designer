============
Installation
============

At the command line::

    $ easy_install msa-designer

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv msa-designer
    $ pip install msa-designer
